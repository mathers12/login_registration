<!DOCTYPE html>
<html>
  <head>
      <% if (authenticated){%>
      <title>user.name</title>
      <%}else{%>
      <title>Prihlasenie</title>
      <%}%>
      <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>

  <% if (authenticated){%>
  <p>Hello, <%= user.name%></p>
  <%}else{%>
  <form action="/login" name="login" method="post">
      <table>
        <td>E-mail</td>
              <td><input type="email" name="email"></td>
          </tr><br>

          <tr>
              <td>Heslo</td>
              <td><input type="password" name="heslo"></td>
          </tr><br>
          <tr>
              <td><input type="submit" value="Prihlásiť sa"></td>
          </tr>
      </table>
  </form>
<p>Ešte nemáte účet vytvorený? <a href="/registration">Zaregistrujte sa</a></p>
  <%}%>
  <%if (success){%>
  <% alert("Verifikacia prebehla uspesne. Teraz sa mozte prihlasit!")%>
  <%}%>
  </body>
</html>
